# Single source of truth for the project version (REVIEW P2: was duplicated
# across 8 build.gradle files with no single bump point). Subprojects inherit
# the root project version; do not set `version` per-module.
version=0.1.0-SNAPSHOT
